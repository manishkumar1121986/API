package com.api.servicebroker.common.helper;
/*package com.mcas.servicebroker.common.helper;

import javax.inject.Named;

import com.mcas.login.domainobject.LoginCredentials;
import com.mcas.login.domainobject.MUserSignInLog;
@Named("serviceBrokerHelper")
public class HelperOfServiceBroker {
	
	
	public MUserSignInLog prepareValidateSessionDomainObject(String sessionId,String userId){
		MUserSignInLog theMUserSignInLog;
		theMUserSignInLog= new MUserSignInLog();
		theMUserSignInLog.setSessionId(sessionId);
		LoginCredentials theLoginCredentials;
		theLoginCredentials= new LoginCredentials();
		theLoginCredentials.setUserId(userId);
		theMUserSignInLog.setLoginCredentials(theLoginCredentials);
		return theMUserSignInLog;
	}

}
*/